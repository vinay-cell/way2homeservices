/*var app = angular.module('way2homeservice', [])
.run(['$anchorScroll', function($anchorScroll) {
	  $anchorScroll.yOffset = 50;   // always scroll by 50 extra pixels
	}])
app.controller('scrollerController', function($scope, $http,$log, $location,$anchorScroll) {
	
	$scope.moveTo = function(dest){
		console.log(dest);
		$anchorScroll(dest);
		
	}
	
});*/
